[package]
name = "clash-common"
version.workspace = true
edition.workspace = true
license = "MIT OR Apache-2.0"
description = "Common utilities, buffers, and data structures for clash-rs"

[dependencies]
bytes = "1"

[dev-dependencies]
tokio = { version = "1", features = ["full"] }
